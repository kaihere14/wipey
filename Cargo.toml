[package]
name = "wipey"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.5", features = ["derive"] }
inquire = "0.9.4"
walkdir = "2"

# Optimised for shipping: smaller downloads, no debug symbols.
[profile.release]
strip = true
lto = true
codegen-units = 1
